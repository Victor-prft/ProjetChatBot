from math import *
from function import *

def idf(repertoire):
    dictionnaire = {}
    for fichier in repertoire:
        for mot in fichier:
            if mot in dico.keys:

            else:
                dictionnaire[mot] = 1